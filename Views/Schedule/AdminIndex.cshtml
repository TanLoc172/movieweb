@* @model IEnumerable<ScheduleAdminItemViewModel>

@{
    ViewData["Title"] = "Quản lý Lịch chiếu";
    Layout = "~/Views/Shared/_LayoutAdmin.cshtml"; // Sử dụng layout của Admin
}

<h1>@ViewData["Title"]</h1>

<p>
    <a asp-controller="Schedule" asp-action="Create" class="btn btn-primary">
        <i class="fas fa-plus"></i> Tạo lịch chiếu mới
    </a>
</p>

<table class="table table-striped table-bordered">
    <thead class="thead-dark">
        <tr>
            <th>Thời gian chiếu</th>
            <th>Tên phim</th>
            <th>Thông tin tập</th>
            <th>Mô tả</th>
            <th>Loại</th>
            <th>Hành động</th>
        </tr>
    </thead>
    <tbody>
        @foreach (var item in Model)
        {
            <tr>
                <td>@item.ScheduledTime.ToLocalTime().ToString("g")</td>
                <td>@Html.DisplayFor(modelItem => item.MovieTitle)</td>
                <td>@Html.DisplayFor(modelItem => item.EpisodeInfo)</td>
                <td>@Html.DisplayFor(modelItem => item.Description)</td>
                <td>@Html.DisplayFor(modelItem => item.EntryType)</td>
                <td>
                    <a asp-controller="Schedule" asp-action="Edit" asp-route-id="@item.Id" class="btn btn-warning btn-sm" title="Sửa"><i class="fas fa-edit"></i></a> |
                    <a asp-controller="Schedule" asp-action="Delete" asp-route-id="@item.Id" class="btn btn-danger btn-sm" title="Xóa"><i class="fas fa-trash"></i></a>
                </td>
            </tr>
        }
    </tbody>
</table> *@




@model IEnumerable<ScheduleAdminItemViewModel>

@{
    ViewData["Title"] = "Quản lý Lịch chiếu";
    Layout = "~/Views/Shared/_LayoutAdmin.cshtml"; // Sử dụng layout của Admin
}

<h1>@ViewData["Title"]</h1>

<p>
    <a asp-controller="Schedule" asp-action="Create" class="btn btn-primary">
        <i class="fas fa-plus"></i> Tạo lịch chiếu mới
    </a>
</p>

        <div class="table-responsive">
            <table class="management-table">
    <thead class="thead-dark">
        <tr>
            <th>Thời gian chiếu</th>
            <th>Tên phim</th>
            <th>Thông tin tập</th>
            <th>Mô tả</th>
            <th>Loại</th>
            <th>Hành động</th>
        </tr>
    </thead>
    <tbody>
        @foreach (var item in Model)
        {
            <tr>
                <td>@item.ScheduledTime.ToLocalTime().ToString("g")</td>
                <td>@Html.DisplayFor(modelItem => item.MovieTitle)</td>
                <td>@Html.DisplayFor(modelItem => item.EpisodeInfo)</td>
                <td>@Html.DisplayFor(modelItem => item.Description)</td>
                <td>@Html.DisplayFor(modelItem => item.EntryType)</td>
                <td>
                    <a asp-controller="Schedule" asp-action="Edit" asp-route-id="@item.Id"  class="btn-action update">Sửa</a>
                    <a asp-controller="Schedule" asp-action="Delete" asp-route-id="@item.Id" class="btn-action delete">Xóa</a>

                </td>
            </tr>
        }
    </tbody>
</table>
        </div>
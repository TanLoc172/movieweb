
<!DOCTYPE html>
<html lang="vi">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Nhập Mã Mời Xem Chung</title>
    <script src="https://cdn.tailwindcss.com"></script>
    <link href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.0.0/css/all.min.css" rel="stylesheet">
    <style>
        .gradient-bg {
            background: linear-gradient(135deg, #667eea 0%, #764ba2 100%);
        }
        .glass-effect {
            backdrop-filter: blur(16px);
            background: rgba(255, 255, 255, 0.1);
            border: 1px solid rgba(255, 255, 255, 0.2);
        }
        .floating {
            animation: float 3s ease-in-out infinite;
        }
        @@keyframes float {
            0%, 100% { transform: translateY(0px); }
            50% { transform: translateY(-10px); }
        }
        .pulse-slow {
            animation: pulse 4s cubic-bezier(0.4, 0, 0.6, 1) infinite;
        }
        .movie-icons {
            position: absolute;
            opacity: 0.1;
            font-size: 2rem;
            color: white;
        }
        .icon-1 { top: 10%; left: 15%; animation: float 3s ease-in-out infinite; }
        .icon-2 { top: 20%; right: 20%; animation: float 3s ease-in-out infinite 1s; }
        .icon-3 { bottom: 30%; left: 10%; animation: float 3s ease-in-out infinite 2s; }
        .icon-4 { bottom: 20%; right: 15%; animation: float 3s ease-in-out infinite 0.5s; }
        .icon-5 { top: 50%; left: 5%; animation: float 3s ease-in-out infinite 1.5s; }
        .icon-6 { top: 60%; right: 8%; animation: float 3s ease-in-out infinite 2.5s; }
    </style>
</head>
@* <body class="min-h-screen gradient-bg relative overflow-hidden"> *@
<body class="min-h-screen gradient-bg relative ">

    <!-- Background animated icons -->
    <div class="movie-icons icon-1"><i class="fas fa-film"></i></div>
    <div class="movie-icons icon-2"><i class="fas fa-video"></i></div>
    <div class="movie-icons icon-3"><i class="fas fa-popcorn"></i></div>
    <div class="movie-icons icon-4"><i class="fas fa-ticket-alt"></i></div>
    <div class="movie-icons icon-5"><i class="fas fa-play-circle"></i></div>
    <div class="movie-icons icon-6"><i class="fas fa-theater-masks"></i></div>

    <!-- Main content -->
    <div class="container mx-auto min-h-screen flex items-center justify-center px-4 py-8 relative z-10">
        <div class="max-w-md w-full">
            <!-- Movie watching illustration -->
            <div class="text-center mb-8">
                <div class="relative inline-block">
                    <div class="w-32 h-32 mx-auto mb-4 floating">
                        <div class="w-full h-full rounded-full bg-white/20 backdrop-blur-sm border border-white/30 flex items-center justify-center shadow-2xl">
                            <i class="fas fa-users text-5xl text-white"></i>
                        </div>
                    </div>
                    <!-- Decorative circles -->
                    <div class="absolute -top-2 -right-2 w-8 h-8 bg-yellow-400 rounded-full pulse-slow"></div>
                    <div class="absolute -bottom-2 -left-2 w-6 h-6 bg-pink-400 rounded-full pulse-slow" style="animation-delay: 1s;"></div>
                </div>
            </div>

            <!-- Main card -->
            <div class="glass-effect rounded-2xl shadow-2xl p-8 transform transition-all duration-300 hover:scale-105">
                <!-- Header -->
                <div class="text-center mb-8">
                    <h1 class="text-3xl font-bold text-white mb-3">
                        <i class="fas fa-door-open mr-2 text-yellow-300"></i>
                        Tham Gia Phòng Xem
                    </h1>
                    <p class="text-white/80 text-lg">
                        Nhập mã mời để cùng bạn bè thưởng thức phim hay
                    </p>
                </div>

                <!-- Invitation code input form -->
                
                <form class="space-y-6"asp-controller="WatchParty" asp-action="Join" method="post" id="joinRoomFormOnPage" class="space-y-6">
            @Html.AntiForgeryToken()
                    <div class="relative">
                        <label for="inviteCode" class="block text-white/90 font-medium mb-3">
                            <i class="fas fa-key mr-2 text-yellow-300"></i>
                            Mã Mời
                        </label>
                        <div class="relative">
                            <input
                                type="text"
                    name="inviteCode"
                    id="inviteCodeInput"
                                placeholder="Nhập mã mời từ bạn bè..."
                                required
                                class="w-full px-4 py-4 bg-white/10 border border-white/20 rounded-xl text-white placeholder-white/50 focus:outline-none focus:ring-2 focus:ring-yellow-300 focus:border-transparent backdrop-blur-sm transition-all duration-300"
                            />
                            <div class="absolute inset-y-0 right-0 pr-3 flex items-center">
                                <i class="fas fa-qrcode text-white/40"></i>
                            </div>
                        </div>
                    </div>

                    <button
                        type="submit"
                        id="submitJoinFormBtn"
                        class="w-full bg-gradient-to-r from-yellow-400 to-orange-500 hover:from-yellow-500 hover:to-orange-600 text-white font-bold py-4 px-6 rounded-xl shadow-lg transform transition-all duration-200 hover:scale-105 hover:shadow-xl focus:outline-none focus:ring-2 focus:ring-yellow-300 focus:ring-offset-2 focus:ring-offset-transparent"
                    >
                        <i class="fas fa-rocket mr-2"></i>
                        Tham Gia Ngay
                    </button>
                </form>

                <!-- Divider -->
                <div class="flex items-center my-8">
                    <div class="flex-1 h-px bg-white/20"></div>
                    <span class="px-4 text-white/60 text-sm">HOẶC</span>
                    <div class="flex-1 h-px bg-white/20"></div>
                </div>

                <!-- Additional options -->
                <div class="grid grid-cols-2 gap-4">
                    <button class="flex flex-col items-center justify-center p-4 bg-white/5 hover:bg-white/10 rounded-xl border border-white/10 transition-all duration-200 group">
                        <i class="fas fa-qr-code text-2xl text-white/70 group-hover:text-white mb-2"></i>
                        <span class="text-white/70 group-hover:text-white text-sm">Quét QR</span>
                    </button>
                    <button class="flex flex-col items-center justify-center p-4 bg-white/5 hover:bg-white/10 rounded-xl border border-white/10 transition-all duration-200 group">
                        <i class="fas fa-plus text-2xl text-white/70 group-hover:text-white mb-2"></i>
                        <span class="text-white/70 group-hover:text-white text-sm">Tạo Phòng</span>
                    </button>
                </div>

                <!-- Info section -->
                <div class="mt-8 p-4 bg-white/5 rounded-xl border border-white/10">
                    <div class="flex items-start space-x-3">
                        <i class="fas fa-info-circle text-blue-300 mt-1"></i>
                        <div>
                            <h3 class="text-white font-medium mb-1">Cách thức hoạt động</h3>
                            <p class="text-white/70 text-sm">
                                Bạn bè sẽ gửi cho bạn mã mời 6-8 ký tự. Nhập mã này để tham gia phòng xem chung và cùng thưởng thức phim với nhau.
                            </p>
                        </div>
                    </div>
                </div>
            </div>

            <!-- Features -->
            <div class="mt-8 grid grid-cols-3 gap-4 text-center">
                <div class="text-white/80">
                    <i class="fas fa-sync-alt text-2xl mb-2 block"></i>
                    <span class="text-sm">Đồng bộ</span>
                </div>
                <div class="text-white/80">
                    <i class="fas fa-comments text-2xl mb-2 block"></i>
                    <span class="text-sm">Chat</span>
                </div>
                <div class="text-white/80">
                    <i class="fas fa-heart text-2xl mb-2 block"></i>
                    <span class="text-sm">Reaction</span>
                </div>
            </div>
        </div>
    </div>

    <script>
        // Add some interactive effects
        document.getElementById('inviteCode').addEventListener('focus', function() {
            this.parentElement.classList.add('scale-105');
        });

        document.getElementById('inviteCode').addEventListener('blur', function() {
            this.parentElement.classList.remove('scale-105');
        });

        // Simulate form submission
        document.querySelector('form').addEventListener('submit', function(e) {
            e.preventDefault();
            const button = this.querySelector('button[type="submit"]');
            const originalText = button.innerHTML;
            
            button.innerHTML = '<i class="fas fa-spinner fa-spin mr-2"></i>Đang tham gia...';
            button.disabled = true;
            
            // Simulate loading
            setTimeout(() => {
                button.innerHTML = '<i class="fas fa-check mr-2"></i>Thành công!';
                setTimeout(() => {
                    button.innerHTML = originalText;
                    button.disabled = false;
                }, 1000);
            }, 2000);
        });
    </script>
</body>
</html>


@* 
<!DOCTYPE html>
<html lang="vi">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>StreamHub - Tham Gia Phòng Xem</title>
    <script src="https://cdn.tailwindcss.com"></script>
    <link href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.0.0/css/all.min.css" rel="stylesheet">
    <link href="https://fonts.googleapis.com/css2?family=Inter:wght@300;400;500;600;700&display=swap" rel="stylesheet">
    <style>
        * {
            font-family: 'Inter', sans-serif;
        }
        
        .cinematic-bg {
            background: 
                radial-gradient(circle at 20% 80%, rgba(120, 50, 150, 0.3) 0%, transparent 50%),
                radial-gradient(circle at 80% 20%, rgba(255, 120, 50, 0.3) 0%, transparent 50%),
                radial-gradient(circle at 40% 40%, rgba(50, 150, 255, 0.2) 0%, transparent 50%),
                linear-gradient(135deg, #0f0f23 0%, #1a1a2e 50%, #16213e 100%);
        }
        
        .glass-morphism {
            backdrop-filter: blur(20px);
            background: rgba(255, 255, 255, 0.05);
            border: 1px solid rgba(255, 255, 255, 0.1);
            box-shadow: 0 25px 50px -12px rgba(0, 0, 0, 0.25);
        }
        
        .neon-glow {
            box-shadow: 0 0 20px rgba(59, 130, 246, 0.3), 0 0 40px rgba(59, 130, 246, 0.1);
        }
        
        .gradient-text {
            background: linear-gradient(135deg, #60a5fa 0%, #a78bfa 50%, #f472b6 100%);
            -webkit-background-clip: text;
            -webkit-text-fill-color: transparent;
            background-clip: text;
        }
        
        .hover-lift {
            transition: all 0.3s cubic-bezier(0.4, 0, 0.2, 1);
        }
        
        .hover-lift:hover {
            transform: translateY(-8px);
            box-shadow: 0 32px 64px -12px rgba(0, 0, 0, 0.4);
        }
        
        .floating-animation {
            animation: float 6s ease-in-out infinite;
        }
        
        @@keyframes float {
            0%, 100% { transform: translateY(0px) rotate(0deg); }
            25% { transform: translateY(-10px) rotate(1deg); }
            50% { transform: translateY(-5px) rotate(0deg); }
            75% { transform: translateY(-15px) rotate(-1deg); }
        }
        
        .pulse-glow {
            animation: pulse-glow 3s ease-in-out infinite;
        }
        
        @@keyframes pulse-glow {
            0%, 100% { 
                box-shadow: 0 0 20px rgba(59, 130, 246, 0.3);
                transform: scale(1);
            }
            50% { 
                box-shadow: 0 0 30px rgba(59, 130, 246, 0.5);
                transform: scale(1.05);
            }
        }
        
        .cinema-particles {
            position: absolute;
            width: 100%;
            height: 100%;
            overflow: hidden;
            pointer-events: none;
        }
        
        .particle {
            position: absolute;
            width: 2px;
            height: 2px;
            background: rgba(255, 255, 255, 0.1);
            border-radius: 50%;
            animation: particle-float 8s linear infinite;
        }
        
        @@keyframes particle-float {
            0% {
                transform: translateY(100vh) translateX(0);
                opacity: 0;
            }
            10% {
                opacity: 1;
            }
            90% {
                opacity: 1;
            }
            100% {
                transform: translateY(-100px) translateX(100px);
                opacity: 0;
            }
        }
        
        .input-glow:focus {
            box-shadow: 0 0 0 2px rgba(59, 130, 246, 0.5), 0 0 20px rgba(59, 130, 246, 0.2);
        }
        
        .btn-primary {
            background: linear-gradient(135deg, #3b82f6 0%, #8b5cf6 50%, #ec4899 100%);
            background-size: 200% 200%;
            animation: gradient-shift 3s ease infinite;
            position: relative;
            overflow: hidden;
        }
        
        .btn-primary::before {
            content: '';
            position: absolute;
            top: 0;
            left: -100%;
            width: 100%;
            height: 100%;
            background: linear-gradient(90deg, transparent, rgba(255, 255, 255, 0.2), transparent);
            transition: left 0.5s ease;
        }
        
        .btn-primary:hover::before {
            left: 100%;
        }
        
        @@keyframes gradient-shift {
            0% { background-position: 0% 50%; }
            50% { background-position: 100% 50%; }
            100% { background-position: 0% 50%; }
        }
        
        .feature-card {
            transition: all 0.3s cubic-bezier(0.4, 0, 0.2, 1);
            border: 1px solid rgba(255, 255, 255, 0.1);
        }
        
        .feature-card:hover {
            transform: translateY(-5px);
            border-color: rgba(59, 130, 246, 0.3);
            box-shadow: 0 10px 25px -5px rgba(59, 130, 246, 0.1);
        }
        
        .logo-text {
            font-weight: 700;
            font-size: 1.5rem;
            letter-spacing: -0.025em;
        }
        
        @@media (max-width: 768px) {
            .glass-morphism {
                margin: 1rem;
                padding: 1.5rem;
            }
        }
        
        .loading-spinner {
            border: 2px solid rgba(255, 255, 255, 0.2);
            border-top: 2px solid #3b82f6;
            border-radius: 50%;
            width: 20px;
            height: 20px;
            animation: spin 1s linear infinite;
        }
        
        @@keyframes spin {
            0% { transform: rotate(0deg); }
            100% { transform: rotate(360deg); }
        }
    </style>
</head>
<body class="min-h-screen cinematic-bg relative overflow-hidden">
    <!-- Animated particles background -->
    <div class="cinema-particles">
        <div class="particle" style="left: 10%; animation-delay: 0s;"></div>
        <div class="particle" style="left: 20%; animation-delay: 1s;"></div>
        <div class="particle" style="left: 30%; animation-delay: 2s;"></div>
        <div class="particle" style="left: 40%; animation-delay: 3s;"></div>
        <div class="particle" style="left: 50%; animation-delay: 4s;"></div>
        <div class="particle" style="left: 60%; animation-delay: 5s;"></div>
        <div class="particle" style="left: 70%; animation-delay: 6s;"></div>
        <div class="particle" style="left: 80%; animation-delay: 7s;"></div>
        <div class="particle" style="left: 90%; animation-delay: 8s;"></div>
    </div>

    <!-- Header -->
    <header class="relative z-20 p-6">
        <div class="container mx-auto">
            <div class="flex items-center justify-between">
                <div class="logo-text gradient-text">
                    <i class="fas fa-play-circle mr-2"></i>
                    StreamHub
                </div>
               
                <button class="md:hidden text-white">
                    <i class="fas fa-bars"></i>
                </button>
            </div>
        </div>
    </header>

    <!-- Main content -->
    <div class="container mx-auto min-h-screen flex items-center justify-center px-4 py-8 relative z-10">
        <div class="max-w-lg w-full">
            <!-- Hero icon -->
            <div class="text-center mb-12">
                <div class="relative inline-block">
                    <div class="w-40 h-40 mx-auto mb-6 floating-animation">
                        <div class="w-full h-full rounded-full glass-morphism flex items-center justify-center neon-glow">
                            <div class="relative">
                                <i class="fas fa-users text-6xl text-white"></i>
                                <div class="absolute -top-2 -right-2 w-6 h-6 bg-green-400 rounded-full pulse-glow"></div>
                            </div>
                        </div>
                    </div>
                    <!-- Floating elements -->
                    <div class="absolute -top-4 -left-4 w-8 h-8 bg-blue-400/20 rounded-full pulse-glow" style="animation-delay: 0.5s;"></div>
                    <div class="absolute -bottom-4 -right-4 w-6 h-6 bg-purple-400/20 rounded-full pulse-glow" style="animation-delay: 1s;"></div>
                    <div class="absolute top-1/2 -right-8 w-4 h-4 bg-pink-400/20 rounded-full pulse-glow" style="animation-delay: 1.5s;"></div>
                </div>
            </div>

            <!-- Main card -->
            <div class="glass-morphism rounded-3xl p-8 hover-lift">
                <!-- Header -->
                <div class="text-center mb-10">
                    <h1 class="text-4xl font-bold text-white mb-4 leading-tight">
                        Tham Gia Phòng Xem
                    </h1>
                    <p class="text-white/60 text-lg font-light">
                        Nhập mã mời để cùng bạn bè thưởng thức những bộ phim hay nhất
                    </p>
                </div>

                <!-- Form -->
                <form class="space-y-8" asp-controller="WatchParty" asp-action="Join" method="post" id="joinRoomFormOnPage">
                    <!-- Invitation code input -->
                    <div class="space-y-3">
                        <label for="inviteCode" class="block text-white/90 font-medium text-sm uppercase tracking-wide">
                            Mã Mời
                        </label>
                        <div class="relative">
                            <input
                                type="text"
                                name="inviteCode"
                                id="inviteCodeInput"
                                placeholder="Nhập mã mời 6-8 ký tự..."
                                required
                                class="w-full px-6 py-4 bg-white/5 border border-white/10 rounded-2xl text-white text-lg placeholder-white/40 focus:outline-none focus:border-blue-400/50 backdrop-blur-sm transition-all duration-300 input-glow"
                                maxlength="8"
                                autocomplete="off"
                            />
                            <div class="absolute inset-y-0 right-0 pr-4 flex items-center">
                                <i class="fas fa-key text-white/30 text-sm"></i>
                            </div>
                        </div>
                    </div>

                    <!-- Submit button -->
                    <button
                        type="submit"
                        id="submitJoinFormBtn"
                        class="w-full btn-primary text-white font-semibold py-4 px-8 rounded-2xl shadow-lg transform transition-all duration-300 hover:scale-[1.02] focus:outline-none focus:ring-2 focus:ring-blue-400 focus:ring-offset-2 focus:ring-offset-transparent relative overflow-hidden"
                    >
                        <span class="relative z-10 flex items-center justify-center">
                            <i class="fas fa-rocket mr-3"></i>
                            Tham Gia Ngay
                        </span>
                    </button>
                </form>

                <!-- Divider -->
                <div class="flex items-center my-10">
                    <div class="flex-1 h-px bg-gradient-to-r from-transparent via-white/20 to-transparent"></div>
                    <span class="px-6 text-white/50 text-sm font-medium">HOẶC</span>
                    <div class="flex-1 h-px bg-gradient-to-r from-transparent via-white/20 to-transparent"></div>
                </div>

                <!-- Quick actions -->
                <div class="grid grid-cols-2 gap-4 mb-8">
                    <button class="feature-card flex flex-col items-center justify-center p-6 bg-white/5 rounded-2xl group">
                        <i class="fas fa-qr-code text-3xl text-white/60 group-hover:text-blue-400 mb-3 transition-colors"></i>
                        <span class="text-white/70 group-hover:text-white text-sm font-medium">Quét QR Code</span>
                    </button>
                    <button class="feature-card flex flex-col items-center justify-center p-6 bg-white/5 rounded-2xl group">
                        <i class="fas fa-plus text-3xl text-white/60 group-hover:text-purple-400 mb-3 transition-colors"></i>
                        <span class="text-white/70 group-hover:text-white text-sm font-medium">Tạo Phòng Mới</span>
                    </button>
                </div>

                <!-- Info section -->
                <div class="p-6 bg-white/5 rounded-2xl border border-white/10">
                    <div class="flex items-start space-x-4">
                        <div class="w-10 h-10 bg-blue-500/20 rounded-full flex items-center justify-center flex-shrink-0">
                            <i class="fas fa-info text-blue-400 text-sm"></i>
                        </div>
                        <div>
                            <h3 class="text-white font-semibold mb-2">Cách thức hoạt động</h3>
                            <p class="text-white/60 text-sm leading-relaxed">
                                Nhận mã mời từ bạn bè, nhập vào ô trên và bắt đầu xem phim cùng nhau với tính năng đồng bộ thời gian thực.
                            </p>
                        </div>
                    </div>
                </div>
            </div>

            <!-- Features grid -->
            <div class="mt-12 grid grid-cols-3 gap-6">
                <div class="text-center group">
                    <div class="w-16 h-16 mx-auto mb-4 bg-white/5 rounded-2xl flex items-center justify-center group-hover:bg-white/10 transition-colors">
                        <i class="fas fa-sync-alt text-2xl text-white/60 group-hover:text-blue-400 transition-colors"></i>
                    </div>
                    <span class="text-white/70 text-sm font-medium">Đồng bộ hoàn hảo</span>
                </div>
                <div class="text-center group">
                    <div class="w-16 h-16 mx-auto mb-4 bg-white/5 rounded-2xl flex items-center justify-center group-hover:bg-white/10 transition-colors">
                        <i class="fas fa-comments text-2xl text-white/60 group-hover:text-purple-400 transition-colors"></i>
                    </div>
                    <span class="text-white/70 text-sm font-medium">Chat thời gian thực</span>
                </div>
                <div class="text-center group">
                    <div class="w-16 h-16 mx-auto mb-4 bg-white/5 rounded-2xl flex items-center justify-center group-hover:bg-white/10 transition-colors">
                        <i class="fas fa-heart text-2xl text-white/60 group-hover:text-pink-400 transition-colors"></i>
                    </div>
                    <span class="text-white/70 text-sm font-medium">Reaction live</span>
                </div>
            </div>
        </div>
    </div>

    <script>
        // Enhanced input interactions
        const inviteInput = document.getElementById('inviteCodeInput');
        const submitBtn = document.getElementById('submitJoinFormBtn');
        
        // Input focus effects
        inviteInput.addEventListener('focus', function() {
            this.parentElement.style.transform = 'scale(1.02)';
        });

        inviteInput.addEventListener('blur', function() {
            this.parentElement.style.transform = 'scale(1)';
        });

        // Real-time input validation
        inviteInput.addEventListener('input', function() {
            const value = this.value.trim();
            const isValid = value.length >= 6 && value.length <= 8;
            
            if (isValid) {
                this.style.borderColor = 'rgba(34, 197, 94, 0.5)';
                submitBtn.disabled = false;
                submitBtn.style.opacity = '1';
            } else {
                this.style.borderColor = 'rgba(255, 255, 255, 0.1)';
                submitBtn.disabled = value.length > 0;
                submitBtn.style.opacity = value.length > 0 ? '0.5' : '1';
            }
        });

        // Form submission with enhanced loading state
        document.getElementById('joinRoomFormOnPage').addEventListener('submit', function(e) {
            e.preventDefault();
            
            const button = submitBtn;
            const originalContent = button.innerHTML;
            
            // Loading state
            button.innerHTML = `
                <span class="relative z-10 flex items-center justify-center">
                    <div class="loading-spinner mr-3"></div>
                    Đang tham gia...
                </span>
            `;
            button.disabled = true;
            button.style.transform = 'scale(0.98)';
            
            // Simulate API call
            setTimeout(() => {
                button.innerHTML = `
                    <span class="relative z-10 flex items-center justify-center">
                        <i class="fas fa-check mr-3"></i>
                        Tham gia thành công!
                    </span>
                `;
                button.style.background = 'linear-gradient(135deg, #10b981 0%, #059669 100%)';
                
                // Reset after success
                setTimeout(() => {
                    button.innerHTML = originalContent;
                    button.disabled = false;
                    button.style.transform = 'scale(1)';
                    button.style.background = '';
                    inviteInput.value = '';
                    inviteInput.style.borderColor = 'rgba(255, 255, 255, 0.1)';
                }, 2000);
            }, 1500);
        });

        // Particle animation enhancement
        function createParticle() {
            const particle = document.createElement('div');
            particle.className = 'particle';
            particle.style.left = Math.random() * 100 + '%';
            particle.style.animationDelay = Math.random() * 8 + 's';
            particle.style.animationDuration = (Math.random() * 5 + 5) + 's';
            
            document.querySelector('.cinema-particles').appendChild(particle);
            
            setTimeout(() => {
                particle.remove();
            }, 10000);
        }

        // Create particles periodically
        setInterval(createParticle, 800);

        // Enhanced mobile responsiveness
        function handleResize() {
            const isMobile = window.innerWidth < 768;
            const mainCard = document.querySelector('.glass-morphism');
            
            if (isMobile) {
                mainCard.style.margin = '1rem';
                mainCard.style.padding = '1.5rem';
            } else {
                mainCard.style.margin = '';
                mainCard.style.padding = '2rem';
            }
        }

        window.addEventListener('resize', handleResize);
        handleResize(); // Initial call
    </script>
</body>
</html> *@